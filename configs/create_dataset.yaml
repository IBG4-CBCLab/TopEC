# @package _global_

# specify here default training configuration
defaults:
  - _self_
  - log_dir: default.yaml
  # enable color logging
  - override hydra/hydra_logging: colorlog
  - override hydra/job_logging: colorlog
# path to original working directory
# hydra hijacks working directory by changing it to the current log directory,
# so it's useful to have this path as a special variable
# learn more here: https://hydra.cc/docs/next/tutorials/basic/running_your_app/working_directory
work_dir: ${hydra:runtime.cwd}


enzyme_csv: ${work_dir}/data/split_csv/full_enzyme_set.csv
weight_path: ${work_dir}/data/split_csv/chemical_weights.csv

dataset_extension: all_enzymes

input_path: ${work_dir}/data/raw/all_structures/
output_path: ${work_dir}/data/processed/${dataset_extension}/

# use `python run.py debug=true` for easy debugging!
# this will run 1 train, val and test loop with only 1 batch
# equivalent to running `python run.py trainer.fast_dev_run=true`
# (this is placed here just for easier access from command line)
debug: False

# pretty print config at the start of the run using Rich library
print_config: True

# disable python warnings if they annoy you
ignore_warnings: True

name: "dataset"